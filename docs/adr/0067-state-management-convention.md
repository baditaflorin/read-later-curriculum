# 0067 - State Management Convention

## Status

Accepted

## Decision

Use:

- React Query for persisted domain data loaded from IndexedDB
- local component state for immediate editing
- persisted UI state helpers for draft/query/shareable session continuity

No additional global state library is introduced in Phase 3.
